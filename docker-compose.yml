version: '3.1'
services:
    blog:
        image: dockercraft/bundler
        container_name: worklog-bundle
        entrypoint:
            - bundler
            - exec
            - jekyll
        working_dir: "/var/www"
        volumes:
            - .:/var/www
        
